# specs/

Feature and requirement specifications. Write a spec **before** implementation starts.

One file per feature: `specs/<NNN>-<short-name>.md` (e.g. `specs/001-traffic-preprocessing-pipeline.md`).

Use [`template.md`](./template.md) as the starting point.
