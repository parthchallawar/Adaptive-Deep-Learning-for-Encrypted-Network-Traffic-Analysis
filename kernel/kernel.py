"""Kaggle kernel entry point.

Runs on Kaggle's hosted GPU with the dataset from kernel-metadata.json's
dataset_sources mounted read-only under /kaggle/input/. Writes checkpoints,
logs, and metrics to /kaggle/working/ so `kaggle kernels output` can pull
them into results/.

Replace this placeholder with a call into src/training once a training
entry point exists.
"""

if __name__ == "__main__":
    print("Kaggle kernel placeholder — wire this up to src/training.")
