# plans/

Implementation plans, derived from a spec in [`specs/`](../specs/). Written before writing code, updated as work progresses.

One file per plan: `plans/<NNN>-<short-name>.md`, matching the spec it implements where possible.

Use [`template.md`](./template.md) as the starting point.
